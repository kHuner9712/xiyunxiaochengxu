# ============================================================
# 禧孕小程序 — 前端生产环境配置模板
# ============================================================
#
# 【用法】
#   1. 复制本文件: cp .env.production.example .env.production
#   2. 替换所有占位符为实际值
#   3. 确认 API 地址以 https:// 开头
#   4. 确认 AppID 为正式 AppID（非测试号）
#
# 【占位符说明】
#   {{API_DOMAIN}}  → 备案域名，如 api.xiyun.com
#   {{WX_APPID}}    → 正式微信小程序 AppID
#
# ⚠️ 此文件包含生产密钥，绝不可提交到 Git！
# ⚠️ API 地址必须使用 HTTPS！
# ⚠️ API 地址不能是 localhost / 127.0.0.1 / 内网 IP！
# ⚠️ AppID 不能是测试号 wxda7779770f53e901！
# ⚠️ 构建时 prod.js 会校验以上规则，不满足将构建失败！
# ============================================================

# 环境标识
UNI_APP_ENV=production

# 后端 API 地址（必填，HTTPS，末尾保留 /）
# 提审/正式阶段：https://备案域名/
# ⚠️ 不允许 http://、localhost、127.0.0.1、内网 IP
UNI_APP_REQUEST_URL=https://{{API_DOMAIN}}/
VUE_APP_UNI_APP_REQUEST_URL=https://{{API_DOMAIN}}/

# 静态资源地址（必填，HTTPS，默认与 API 地址相同，CDN 部署时单独配置）
UNI_APP_STATIC_URL=https://{{API_DOMAIN}}/
VUE_APP_UNI_APP_STATIC_URL=https://{{API_DOMAIN}}/

# 上传地址（默认与 API 地址相同，OSS 部署时单独配置）
# ⚠️ 如单独配置，必须使用 HTTPS
UNI_APP_UPLOAD_URL=https://{{API_DOMAIN}}/

# 微信小程序 AppID（必填，正式 AppID）
# ⚠️ 必须与 manifest.json / project.config.json 中的 AppID 一致！
# ⚠️ 不允许使用测试号 wxda7779770f53e901！
UNI_APP_WX_APPID={{WX_APPID}}
VUE_APP_UNI_APP_WX_APPID={{WX_APPID}}

# 定位权限（生产环境按需开启）
# 活动签到/地址选择需要定位时设为 true
# 一期如不强制的定位可设为 false，减少权限弹窗
UNI_APP_ENABLE_LOCATION=true
