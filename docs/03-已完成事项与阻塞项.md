# 已完成事项与阻塞项

## 已完成事项

### ✅ 1. 目录结构创建
- 创建 `shopxo-backend/`、`shopxo-uniapp/`、`docs/` 三级目录

### ✅ 2. 后端代码拉取与依赖安装
- 从 `https://github.com/gongfuxiang/shopxo` 克隆代码到 `shopxo-backend`
- Composer 依赖已安装（vendor/ 目录完整）

### ✅ 3. 前端代码拉取与依赖安装
- 从 `https://github.com/gongfuxiang/shopxo-uniapp` 克隆代码到 `shopxo-uniapp`
- npm 依赖已安装（3 个包，0 漏洞）

### ✅ 4. Docker 环境搭建
- 创建 docker-compose.yml（Nginx + PHP-FPM + MySQL）
- 创建 Nginx 配置（含 ShopXO rewrite 规则）
- 创建 PHP 8.1-FPM Dockerfile（含所有必要扩展）
- 三个容器均正常运行

### ✅ 5. 后端安装完成
- 通过安装向导完成数据库初始化（82 张表）
- 管理员账号已创建（admin / admin123456）
- 管理后台入口已重命名为 `adminwlmqhs.php`
- 前台首页可正常访问
- API 接口可正常返回数据

### ✅ 6. 前端 API 对接配置
- 修改 `App.vue` 中 `request_url` 和 `static_url` 指向 `http://localhost:8080/`
- 微信小程序 `urlCheck` 已设为 `false`

### ✅ 7. 项目文档生成
- 01-项目结构说明.md
- 02-本地启动说明.md
- 03-已完成事项与阻塞项.md

---

## 阻塞项 / 待手工处理

### 🔴 1. 微信小程序 AppID 需替换
- **现状**：manifest.json 中 `mp-weixin.appid` 为官方演示值 `wxda7779770f53e901`
- **需要**：替换为你自己在微信公众平台注册的小程序 AppID
- **文件**：`shopxo-uniapp/manifest.json` 第 181 行
- **影响**：不替换无法在真机预览/发布

### 🔴 2. HBuilderX 需手动安装
- **现状**：uni-app 项目需要 HBuilderX 来编译运行
- **需要**：下载安装 HBuilderX（https://www.dcloud.io/hbuilderx.html）
- **影响**：不安装无法编译前端项目

### 🟡 3. 微信开发者工具需手动安装
- **现状**：微信小程序开发需要微信开发者工具
- **需要**：下载安装微信开发者工具（https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html）
- **影响**：不安装无法预览微信小程序

### 🟡 4. HTTPS 证书（生产环境）
- **现状**：本地开发使用 HTTP，微信小程序正式环境要求 HTTPS
- **需要**：配置 SSL 证书 + Nginx HTTPS
- **影响**：仅影响正式发布，本地开发不受影响

### 🟡 5. 微信支付/登录配置
- **现状**：manifest.json 中微信支付/登录的 appid/appsecret 为官方演示值
- **需要**：替换为你自己的微信开放平台配置
- **影响**：微信登录、微信支付功能不可用

---

## 假设记录

以下是在执行过程中做的合理假设：

1. **使用 Docker 替代 phpStudy/XAMPP**：本地未安装 PHP/MySQL 环境，选择 Docker 方案
2. **MySQL 主机名使用 `mysql`**：Docker 网络内容器间通过服务名访问
3. **管理员密码设为 `admin123456`**：仅用于本地开发，生产环境必须修改
4. **MySQL root 密码设为 `root123456`**：仅用于本地开发
5. **使用 PHP 8.1-FPM Alpine 镜像**：满足 ShopXO >= 8.0.2 的要求
6. **前端 API 地址使用 `http://localhost:8080/`**：本地开发模式，末尾 `/` 必须保留
7. **微信小程序 AppID 暂用官方演示值**：后续需替换

---

## 后续二开建议

1. **接入 DIY 装修仓库**：`https://github.com/gongfuxiang/shopxo-diy`
2. **接入 Form 表单仓库**：`https://github.com/gongfuxiang/shopxo-form`
3. **配置微信支付**：在管理后台 → 支付管理中配置微信支付参数
4. **配置邮件/短信**：在管理后台 → 系统设置中配置邮件和短信服务
5. **自定义主题**：在管理后台 → 主题管理中安装/自定义主题
6. **安装插件**：在应用市场中安装所需插件（分销、秒杀、门店等）
