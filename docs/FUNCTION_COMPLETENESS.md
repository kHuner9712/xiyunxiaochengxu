# 功能完整度评估（2026-05-27）

> 评估口径：是否达到“可部署到预生产并进行真机验收”的商用上线前状态。

## 小程序端

- 完整度：88%
- 已完成：登录态与过期处理、商品浏览与合规信息展示、购物车增删改查、下单金额试算、支付结果服务端查询、售后申请与图片上传、客服入口、协议入口、分享非强制。
- 缺失：真实客服电话/微信与退货地址最终值待运营确认；真机支付/退款实测记录待补。
- 是否阻塞上线：是
- 建议优先级：P0

## 管理后台

- 完整度：84%
- 已完成：商品增改查、上/下架、高合规字段录入、订单管理（发货/取消/确认收货）、售后审核与退款、用户/会员/积分、优惠券、内容管理、自提点与核销、系统配置、业务事件监控、订单导出。
- 缺失：关键运营流程（真实退款链路、高合规上架全流程）仍需预生产联调验收。
- 是否阻塞上线：是
- 建议优先级：P0

## 后端 API

- 完整度：90%
- 已完成：生产环境关键配置校验、支付回调验签与金额校验、支付/退款幂等、库存并发保护、取消订单资源释放、优惠券/积分锁定与释放、自动关单/自动收货任务、上传 MIME+魔数校验、权限与 DTO 校验、业务事件可观测。
- 缺失：真实商户证书与生产密钥接入后的端到端支付/退款验收记录。
- 是否阻塞上线：是
- 建议优先级：P0

## 部署与运维

- 完整度：82%
- 已完成：`release-check` 门禁、`release:check:prod` 强门禁、部署配置检查脚本、Docker Compose 配置校验、预生产部署脚本。
- 缺失：服务器真实 `.env.production` 与证书落地、DNS 与 HTTPS 线上验证执行记录。
- 是否阻塞上线：是
- 建议优先级：P0

## 合规与审核材料

- 完整度：70%
- 已完成：协议页面去占位并改为配置字段、食品安全提示语、审核材料包框架、运营待填清单。
- 缺失：食品/奶粉/保健品资质文件、备案号、统一社会信用代码、审核账号与审核备注最终稿。
- 是否阻塞上线：是
- 建议优先级：P0

## 运营准备

- 完整度：65%
- 已完成：运营待填项清单、真机验收清单、部署 Runbook。
- 缺失：商品与营销正式数据、客服与售后规则最终口径、审核上传材料。
- 是否阻塞上线：是
- 建议优先级：P0

## 当前结论

- 技术上可进入预生产部署与真机验收。
- 仍不满足直接正式发布条件。
- 阻塞项以 `docs/OPERATOR_REQUIRED.md` 为准。

## 本次新增已完成能力（2026-05-29）

1. 订单完成发奖逻辑统一为单一路径，快递确认、自提核销、自动完成均幂等。
2. 取消/超时关闭/退款回滚链路加入销量防负保护（`sales >= 0`）。
3. 支付回调新增补偿任务机制（已取消订单收到支付成功回调）。
4. 支付/退款回调支持多平台证书序列号验签（静态多证书模式）。
5. 类目新增 `complianceConfig`，商品上架由类目配置 + 商品合规字段共同判定。
6. 小程序生产构建新增 `VITE_API_BASE_URL` 强门禁校验。
# 2026-05-29 第二轮上线前修复状态

- 已完成：`PickupStoreModule` 引入 `OrderModule`，`PickupStoreService` 注入链路可通过 Nest TestingModule 实例化测试。
- 已完成：支付补偿任务 `PaymentCompensationTask` 已在 Prisma schema 和 migration 中落地，并补充查询/处理测试。
- 已完成：后台分类更新与多个 admin `update` API 已避免将 `id` 放入 body，降低生产环境 `forbidNonWhitelisted` 400 风险。
- 已完成：订单取消/超时关闭/退款回滚路径销量扣减统一为不低于 `0` 的安全实现。
- 已完成：本地上传删除逻辑改为基于 `UPLOAD_DIR` 的真实路径解析，并增加路径穿越防护与自定义目录测试。
- 结论：当前为“可进入真实预生产部署与正式商用上线验收”的代码候选状态，不等于“已可正式上线”。
